# aayu-http

The `aayu-http` package allows your AAYU code to natively make external API requests.

## Functions

- `http.get_request({"url": "..."})`: Makes a GET request.
- `http.post_request({"url": "...", "body": "..."})`: Makes a POST request.
