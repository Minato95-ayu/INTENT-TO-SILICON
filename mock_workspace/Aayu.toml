[package]
name = "mock_project"
version = "1.0.0"
edition = "2026"

[authors]
names = ["Test Author"]

[build]
entry = "src/main.aayu"

[dependencies]
# We don't have external deps in mock_workspace yet, but let's add one to test
mathlib = "2.1.0"
