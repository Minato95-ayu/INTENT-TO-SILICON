Page Settings {
    Row {
        Sidebar {
            Button "Dashboard" { navigate("/") }
            Button "Settings" { navigate("/settings") }
        }
        Column {
            Text "Settings Page"
        }
    }
}
