version: '3.8'

services:
  aayu-app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - AAYU_PORT=8080
      - AAYU_ENV=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
